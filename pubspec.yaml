name: rick_hub
description: Wabalabadabdab
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  bloc: ^8.1.2
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  uuid: ^3.0.0
  infinite_scroll_pagination:
  flutter_svg:
  google_fonts:
  refresh_loadmore :
  http:
  dio:

dev_dependencies:
  bloc_test: ^9.1.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

